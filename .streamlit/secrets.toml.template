# ── QUIRA OS — Template de Secrets ───────────────────────────────────────────
# INSTRUCCIONES:
#   1. Copia este archivo y renómbralo a: secrets.toml  (en la misma carpeta)
#   2. Reemplaza los valores de placeholder con los reales
#   3. NUNCA subas secrets.toml al repositorio (está en .gitignore)
#
# Para Streamlit Community Cloud:
#   Pega el contenido en: App Settings → Secrets
# ─────────────────────────────────────────────────────────────────────────────

# Anthropic API Key (Sentinel IA)
# Obtener en: https://console.anthropic.com/
ANTHROPIC_API_KEY = "sk-ant-REEMPLAZA-CON-TU-KEY"

# Contraseña global del PMV (opcional — si quieres cambiarla desde secrets)
# Si no se define, usa la de config.py ("quira2026")
# PMV_PASSWORD = "tu-contraseña-segura"
